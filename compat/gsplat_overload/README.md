# gsplat overload

This distribution supplies the `gsplat` import tree by forwarding modules to
the matching `ptxsplat` release. It must not be installed alongside the
official `gsplat` distribution.
